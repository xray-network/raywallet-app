const actions = {
  SET_STATE: 'transactions/SET_STATE',
  CHANGE_SETTING: 'transactions/CHANGE_SETTING',
  BUILD_TX: 'transactions/BUILD_TX',
  SEND_TX: 'transactions/SEND_TX',
}

export default actions
